# AI 模型可视化 Prompt 参考

---

## 基础生成

```
生成纯前端页面演示{模型名称}的工作原理，使用偏暗的炫酷色调，
多一些图形效果，要求1000行以上代码
```

```
生成网页 {模型名称}，使用偏暗的色调，
主要注重图形与动态显示效果，在一个屏幕内显示，
要求完成度高，1000行以上代码
```

---

## 动画细化

```
对文件进行动画效果的精细化优化，要求实现页面中所有功能模块及其包含的
小元素按预设顺序依次出现的动画效果。具体实施要求如下：
1. 为页面中各大功能模块建立明确的层级动画关系，确保父模块与子元素的动画衔接自然
2. 按相同精细度标准，为页面其他功能模块设计并实现类似的有序动画序列
3. 确保所有动画效果具有统一的时间曲线和过渡节奏
4. 动画持续时间、延迟时间和过渡效果应保持一致的设计语言
```

```
我发现"{模块名}"模块内的动画并没有按正确的顺序依次出现，
应该按照：{步骤1}、（箭头）、{步骤2}、（箭头）、{步骤3}的顺序
```

---

## 数据流动效果

```
为虚线箭头元素添加持续循环的流动的动画效果，
使箭头在完成初始出现动画后能够保持动态状态。
```

```
将箭头替换为虚线箭头，并添加持续循环的流动动画效果。
```

---

## 交互增强

```
为X模块整体添加3D旋转效果，实现鼠标往页面左侧移动时该模块Y轴向左，
鼠标往页面右侧移动时该模块Y轴向右
```

```
为所有有鼠标指针hover后发光效果的元素，设置为默认就有hover的发光效果
```

```
为网页中的元素添加"发光"或"泛光"的效果
```

---

## 加载动画

```
为该网页添加一个打开时的加载页面，加载时间为3秒钟
```

```
帮我实现在打开这个网页时，各个元素依次"缓入"出现的动画效果，
且在切换场景时元素内容变化时也添加变化的动画效果
```
